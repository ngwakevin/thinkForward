# .deployment file for Azure App Service
# This tells the deployment system how to deploy the app

[config]
# Optimized deployment settings for Node.js applications
SCM_DO_BUILD_DURING_DEPLOYMENT=false
ENABLE_ORYX_BUILD=false

# For ZIP deploy, use custom startup command
command = node server.js