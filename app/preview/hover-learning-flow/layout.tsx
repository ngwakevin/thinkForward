import React from "react";

export const metadata = {
  title: "Preview – Hover Learning Flow",
  description: "Interactive hover-only preview of the learning journey flow (Sign up → Pick course → Learn → Achieve).",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
