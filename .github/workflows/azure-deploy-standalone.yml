name: Deploy to Azure App Service

on:
  push:
    branches:
      - main  # Change to your production branch if different
      - azure-deploy-clean
  workflow_dispatch:

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run Prisma generate
        run: npx prisma generate

      - name: Build Next.js application
        run: npm run build
        env:
          NODE_ENV: production

      - name: Prepare for deployment
        run: |
          # Create deployment package for standalone mode
          mkdir -p deployment
          cp -r .next/standalone/* deployment/
          cp -r .next/static deployment/.next/
          mkdir -p deployment/public
          cp -r public/* deployment/public/
          cp scripts/azure-standalone-startup.sh deployment/
          chmod +x deployment/azure-standalone-startup.sh

      - name: Deploy to Azure App Service
        uses: azure/webapps-deploy@v2
        with:
          app-name: ${{ secrets.AZURE_WEBAPP_NAME }}
          publish-profile: ${{ secrets.AZURE_PUBLISH_PROFILE }}
          package: ./deployment